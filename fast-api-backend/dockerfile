# Use an official, lightweight Python image
FROM python:3.10-slim

# Set the working directory inside the container
WORKDIR summary/main.py

# Copy the requirements file first to leverage Docker's layer caching
# This way, it only re-installs packages if requirements.txt changes
COPY requirements.txt .

# Install your Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy your application code into the container
COPY . .

# IMPORTANT: Expose the port your app will run on.
# AWS Elastic Beanstalk's Docker platform expects the app to run on port 80 by default.
EXPOSE 80

# Command to run your FastAPI app with uvicorn
# We use --host 0.0.0.0 to make it accessible from outside the container
# We use --port 80 to match what Elastic Beanstalk expects
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "80"]